# Test generated by RoostGPT for test demo56 using AI Type Azure Open AI and AI Model roost-gpt

config:
  target: "https://dummy-api-endpoint.com"
  phases:
    - duration: 10
      arrivalRate: 10

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: "Test Product"
            price: 10.99
      - get:
          url: "/products/{{ response.body._id }}"
          headers:
            Accept: "application/json"
      - expect:
          statusCode: 200
          json:
            name: "Test Product"
            price: 10.99

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: "Test Product"
          expect:
            statusCode: 400
            json:
              error: "Validation failed: Price is required."

  - name: "API request with edge case"
    flow:
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: "Test Product"
            price: "Special characters or large data"
      - get:
          url: "/products/{{ response.body._id }}"
          headers:
            Accept: "application/json"
      - expect:
          statusCode: 200
          json:
            name: "Test Product"
            price: "Special characters or large data"
