# Test generated by RoostGPT for test demo56 using AI Type Azure Open AI and AI Model roost-gpt


  Please regenerate the following artillery test case, as the generated yaml file is not a valid yaml due to following error :
  ERRORS:
  config:
  target: "http://localhost:8080" # Replace with the actual API endpoint URL
  phases:
    - duration: 1
      arrivalRate: 1

scenarios:
  - scenario: "Successful API request"
    flow:
      - post:
          url: "/product/1"
          headers:
            Content-Type: "application/json"
          json:
            id: 1
            name: "Example Product"
            price: 10.99
      - expect:
          statusCode: 200
          json:
            id: 1
            name: "Example Product"
            price: 10.99

  - scenario: "API request with validation failure"
    flow:
      - post:
          url: "/product/2"
          headers:
            Content-Type: "application/json"
          json:
            name: "Invalid Product"
            price: 5.99
      - expect:
          statusCode: 400
          json:
            error: "Invalid payload"

  - scenario: "API request with edge case"
    flow:
      - post:
          url: "/product/3"
          headers:
            Content-Type: "application/json"
          json:
            id: 3
            name: "Product with special characters"
            price: 99999999999999999.99
      - expect:
          statusCode: { from: 200, to: 204 }
          pattern: "Product details: .*"

  TEST CASE:
  