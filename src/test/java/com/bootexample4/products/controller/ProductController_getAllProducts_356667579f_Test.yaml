# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://dummyapi.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/products"
          expect:
            - statusCode: 200
            - hasProperty: "products"
            
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/products"
          headers:
            invalidHeader: "invalidValue"
          expect:
            - statusCode: 400
            - hasProperty: "errorMessage"
            
  - name: "API request with edge case"
    flow:
      - get:
          url: "/products?name=veryLongProductNameWithSpecialCharacters!@#$%^&*()"
          expect:
            - statusCode: 200
            - hasProperty: "products"
