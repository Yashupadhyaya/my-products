# Test generated by RoostGPT for test demo56 using AI Type Azure Open AI and AI Model roost-gpt


  Please regenerate the following artillery test case, as the generated yaml file is not a valid yaml due to following error :
  ERRORS:
  config:
  target: "http://localhost:8080" # Replace with the actual API endpoint

scenarios:
  - name: 'Successful API request'
    flow:
      - post:
          url: "/{id}"
          json:
            name: "Sample Product"
            description: "Sample description"
            price: 10.99
      - expect:
          statusCode: 200
          json: 
            name: "Sample Product"
            description: "Sample description"
            price: 10.99
 
  - name: 'API request with validation failure'
    flow:
      - post:
          url: "/{id}"
          json:
            name: ""
            description: "Sample description"
            price: 10.99
      - expect:
          statusCode: 400
          json:
            error: "Invalid request. Name field is required."
  
  - name: 'API request with edge case'
    flow:
      - post:
          url: "/{id}"
          json:
            name: "Special characters: @#$%^&*()"
            description: "Large data: <content>"
            price: 10000
      - expect:
          statusCode:
            min: 200 # Assuming a successful request will have a status code between 200-299
            max: 299

  TEST CASE:
  