# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "ProductController_GetMapping_8904bc73fc_Test.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "product"
            - hasHeader: "Content-Type"
            
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid-url"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "product"
            - hasHeader: "Content-Type"
            - equals: 
                product:
                  id: "{{ id }}"
                  name: "Product with special characters or large data"
