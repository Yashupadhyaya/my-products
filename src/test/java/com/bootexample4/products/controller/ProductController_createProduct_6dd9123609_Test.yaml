# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/product"
          json:
            name: "Product1"
            description: "This is a test product"
            price: 100
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - equals:
                - "name"
                - "Product1"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/product"
          json:
            name: ""
            description: "This is a test product"
            price: 100
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
                - "error"
                - "Validation failed"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/product"
          json:
            name: "Product1"
            description: "This is a test product with a very long description that exceeds the maximum allowed length. This is a test product with a very long description that exceeds the maximum allowed length. This is a test product with a very long description that exceeds the maximum allowed length."
            price: 100
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
                - "error"
                - "Description exceeds maximum length"
