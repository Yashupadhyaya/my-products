# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/products"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "ProductController_getAllProducts_e267ceea76_Test.csv"
    fields:
      - "ProductID"
      - "ProductName"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "products"
            - hasHeader: "Content-Type"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidURL"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ ProductID }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ gt(200) }}"
            - statusCode: "{{ lt(300) }}"
            - hasProperty: "products"
            - hasHeader: "Content-Type"
