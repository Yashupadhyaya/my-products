# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/api/products"
  plugins:
    expect: {}
  payload:
    path: "ProductController_getAllProducts_e267ceea76_Test.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get: "/"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "products"
          - hasProperty: "products[0].id"
          - hasProperty: "products[0].name"
          - hasProperty: "products[0].description"
          - hasProperty: "products[0].price"

  - name: "API request with validation failure"
    flow:
      - get: "/invalid"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - equals: ["message", "Invalid URL"]

  - name: "API request with edge case"
    flow:
      - get: "/"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: {"$between": [200, 299]}
          - hasProperty: "products"
          - hasProperty: "products[0].id"
          - hasProperty: "products[0].name"
          - hasProperty: "products[0].description"
          - hasProperty: "products[0].price"
