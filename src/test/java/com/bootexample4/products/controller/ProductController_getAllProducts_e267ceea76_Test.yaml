# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "ProductController_getAllProducts_e267ceea76_Test.csv"
    fields:
      - "productId"
      - "productName"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/products"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "products"
            - hasProperty: "products[0].productId"
            - hasProperty: "products[0].productName"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_endpoint"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
            - hasProperty: "productName"
            - equals: ["{{ productId }}", "{{ response.body.productId }}"]
            - equals: ["{{ productName }}", "{{ response.body.productName }}"]
