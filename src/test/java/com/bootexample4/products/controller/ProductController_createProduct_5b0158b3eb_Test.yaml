# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}/string1/string2.../stringN"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "ProductController_createProduct_5b0158b3eb_Test.csv"
    fields: ["field1", "field2", "field3"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/createProduct"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
            field3: "{{ field3 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "product"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/createProduct"
          json:
            field1: ""
            field2: "{{ field2 }}"
            field3: "{{ field3 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/createProduct"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
            field3: "This is a string with special characters and a very long length..."
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode:
                inRange: [200, 500]
            - hasProperty: "product"
