# Test generated by RoostGPT for test demo56 using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  plugins:
    expect: {}
  payload:
    path: "ProductController_createProduct_5b0158b3eb_Test.csv"
    fields: ["productId", "productName", "productDescription", "productPrice"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/v1/product"
          json:
            productId: "{{ productId }}"
            productName: "{{ productName }}"
            productDescription: "{{ productDescription }}"
            productPrice: "{{ productPrice }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "productId"
            - hasProperty: "productName"
            - hasProperty: "productDescription"
            - hasProperty: "productPrice"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/v1/product"
          json:
            productId: "{{ productId }}"
            productName: "{{ productName }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/v1/product"
          json:
            productId: "{{ productId }}"
            productName: "{{ productName }}"
            productDescription: "{{ productDescription }}"
            productPrice: "{{ productPrice }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ statusCode }}"
            - hasProperty: "productId"
            - hasProperty: "productName"
            - hasProperty: "productDescription"
            - hasProperty: "productPrice"
