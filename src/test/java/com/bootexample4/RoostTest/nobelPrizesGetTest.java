// Test generated by RoostGPT for test postman-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

// RoostTestHash=4940f5c55b

private void assertResponse(Response response) {
    int statusCode = response.getStatusCode();

    String code = response.jsonPath().get("code");
    String message = response.jsonPath().get("message");

    assertThat(code, instanceOf(String.class));
    assertThat(message, instanceOf(String.class));

    switch (statusCode) {
        case 200:
            assertOkResponse(response);
            break;
        case 400:
        case 404:
        case 422:
            break;
        default:
            fail("Unexpected status code: " + statusCode);
    }
}
