# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=36caaf0283
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "store_order_orderId_delete_success.csv"
    fields:
      - "orderId"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Delete purchase order by ID"
    flow:
      - log: "Performing delete operation on Purchase Order ID: {{ orderId }}"
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "orderId"
      - think: 1
