# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/functions/{file-id}_get for http method type GET in artillery framework
# RoostTestHash=bcbad18715
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "forms_functions_file-id_get_others.csv"
    fields:
      - "fileId"
      - "AuthorizationToken"
      - "CorrelationId"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.example.com"
      variables:
        AUTH_TOKEN: "YOUR_DEV_TOKEN"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending GET request with invalid fileId"
      - get:
          url: "/forms/functions/{{ fileId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "{{ CorrelationId }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending GET request without Authorization header"
      - get:
          url: "/forms/functions/{{ fileId }}"
          headers:
            X-HSBC-Request-Correlation-Id: "{{ CorrelationId }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending GET request with invalid URL"
      - get:
          url: "/forms/invalid/{{ fileId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "{{ CorrelationId }}"
          expect:
            - statusCode: 404
      - think: 1
