# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.wcvjwcvwcw_API_HOST }}"
  plugins:
    expect: {}
  payloads:
  - name: "employees_post_others.csv"
    path: "./employees_post_others.csv"
    fields: ["email", "jobTitle", "name"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses Scenario"
    flow:
      - log: "Post Request with an invalid payload"
      
      - post:
         url: "/employees"
         headers:
          Authorization: "Bearer {{ $processEnvironment.wcvjwcvwcw_AUTH_TOKEN }}"
         json:
          email: "{{ $randomEmail() }}"
          jobTitle: "{{ jobTitle }}"
          name: "{{ name }}"
          
      - expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "errors"
      - think: 1

      - log: "Put Request with an incorrect authentication token"
      
      - put:
          url: "/employees/2"
          headers:
            Authorization: "Bearer incorrect_token"
          json:
            email: "{{ $randomEmail() }}"
            jobTitle: "System Analyst"
            name: "John Smith"
      
      - expect:
            - statusCode: 401
      - think: 1

      - log: "Delete Request to an invalid URL"
      
      - delete:
          url: "/wrong_url/1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.wcvjwcvwcw_AUTH_TOKEN }}"
          
      - expect:
            - statusCode: 404
      - think: 1
