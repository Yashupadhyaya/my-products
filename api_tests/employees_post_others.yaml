# ********RoostGPT********

# Test generated by RoostGPT for test api-spec-test-v1 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "employees_post_others.csv"
    fields:
      - "id"
      - "jobTitle"
      - "name"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
  environments:
    development:
      target: "http://localhost:3000"
      $processEnvironment:
        AUTH_TOKEN: "your_development_auth_token"
    production:
      target: "http://api.yourapp.com"
      $processEnvironment:
        AUTH_TOKEN: "your_production_auth_token"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Invalid POST /employees request"
      - post:
          url: "/employees"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Invalid URL"
      - get:
          url: "/invalid"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Missing Authorization header"
      - post:
          url: "/employees"
          json:
            id: "{{ id }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
      - think: 1
