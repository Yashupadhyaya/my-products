# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "employees_post_others.csv"
    fields:
      - "email"
      - "jobTitle"
      - "name"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      - log: "Attempting to add an employee with invalid payload"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to add an employee without authorization token"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to add an employee with incorrect URL"
      - post:
          url: "/employee"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
