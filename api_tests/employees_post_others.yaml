# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "employees_post_others.csv"
    fields:
      - "id"
      - "jobTitle"
      - "name"
      - "email"
  plugins:
    expect: {}
    
scenarios:
  - name: "Error scenario"
    flow:
      - log: "Testing POST request with invalid URL"
      - post:
        url: "/inxalid_url"
        headers:
          Content-Type: "application/json"
          Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        json:
          id: "{{ id }}"
          jobTitle: "{{ jobTitle }}"
          name: "{{ name }}"
          email: "{{ email }}"
        expect:
          - statusCode: 404
      - think: 1

      - log: "Testing POST request with missing jobTitle in payload"
      - post:
        url: "/employees"
        headers:
          Content-Type: "application/json"
          Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        json:
          id: "{{ id }}"
          name: "{{ name }}"
          email: "{{ email }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - equals: ["error", "Missing jobTitle"]
      - think: 1

      - log: "Testing POST request with incorrect Authorization token"
      - post:
        url: "/employees"
        headers:
          Content-Type: "application/json"
          Authorization: "Bearer invalid_token"
        json:
          id: "{{ id }}"
          jobTitle: "{{ jobTitle }}"
          name: "{{ name }}"
          email: "{{ email }}"
        expect:
          - statusCode: 401
      - think: 1
    
      - log: "Testing POST request with invalid email in payload"
      - post:
        url: "/employees"
        headers:
          Content-Type: "application/json"
          Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        json:
          id: "{{ id }}"
          jobTitle: "{{ jobTitle }}"
          name: "{{ name }}"
          email: "invalid email"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - equals: ["error", "Invalid email"]
      - think: 1
