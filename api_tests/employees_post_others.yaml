# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "employees_post_others.csv"
    fields:
      - "email"
      - "jobTitle"
      - "name"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      - log: "Attempting to add an employee with missing required fields"
      - post:
          url: "/employees"
          json:
            email: "{{ email }}"
            name: "{{ name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to add an employee with invalid email format"
      - post:
          url: "/employees"
          json:
            email: "invalid email"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to add an employee with unauthorized access"
      - post:
          url: "/employees"
          json:
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to add an employee to an invalid URL"
      - post:
          url: "/invalid_url"
          json:
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
