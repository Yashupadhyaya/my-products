# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "employees_post_others.csv"
    fields:
      - "email"
      - "jobTitle"
      - "name"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Invalid API usage Test Scenario"
    flow:
      - log: "Test request with missing required field"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{$email}}"
            jobTitle: "{{$jobTitle}}"
      - expect:
            - statusCode: 400
      - think: 1

      - log: "Test request with invalid email format"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "invalidEmail"
            jobTitle: "{{$jobTitle}}"
            name: "{{$name}}"
      - expect:
            - statusCode: 400
      - think: 1

      - log: "Test request with invalid authentication token"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalidToken"
          json:
            email: "{{$email}}"
            jobTitle: "{{$jobTitle}}"
            name: "{{$name}}"
      - expect:
            - statusCode: 401
      - think: 1

      - log: "Test request with invalid URL"
      - post:
          url: "/invalidURL"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{$email}}"
            jobTitle: "{{$jobTitle}}"
            name: "{{$name}}"
      - expect:
            - statusCode: 404
      - think: 1
