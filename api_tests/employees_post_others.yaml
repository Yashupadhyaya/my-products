# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "employees_post_others.csv"
    fields:
      - "email"
      - "jobTitle"
      - "name"
  plugins:
    expect: {}
  
scenarios:
  - name: "Invalid API usage Test"
    flow:
      - log: "Testing Post request with missing required fields"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "d290f1ee-6c54-4b01-90e6-d701748f0851"
            name: "{{ name }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["message", "Missing required field: email"]
      - think: 1

      - log: "Testing Post request with invalid token"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer InvalidToken"
          json:
            id: "d290f1ee-6c54-4b01-90e6-d701748f0851"
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: ["message", "Invalid token"]
      - think: 1

      - log: "Testing Post request with invalid URL"
      - post:
          url: "/invalidURL"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "d290f1ee-6c54-4b01-90e6-d701748f0851"
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 404
      - think: 1
