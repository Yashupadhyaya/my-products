# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/{username}_get for http method type GET in artillery framework
# RoostTestHash=b96457affd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_username_get_others.csv"
    fields:
      - "username"
    skipHeader: true
    # Corrected the delimiter to standard comma
    delimiter: ","

scenarios:
  - name: Non-Success API responses artillery test scenario
    flow:
      - log: "Starting execution of artillery script for non-success API responses"
      
      - get:
          url: "/invalid_endpoint_that_doesnt_exist"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

      - think: 5

      - post:
          url: "/v2/user"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: 0
            username: "{{ username }}"
            firstName: "John"
            lastName: "Doe"
            email: "johndoe@example.com"
            password: "password"
            phone: "1234567890"
            userStatus: 0
          expect:
            - statusCode: 405

      - think: 5

      - get:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "username"
            - hasProperty: "id"
        
      - think: 5

      - put:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            firstName: "John"
            lastName: "Doe"
            email: "johndoe@example.com"
            password: "password"
            phone: "1234567890"
            userStatus: 0
          expect:
            - statusCode: 405

      - think: 5

      - delete:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
