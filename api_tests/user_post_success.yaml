# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user_post for http method type POST in artillery framework
# RoostTestHash=d815599fc6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_post_success.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - flow:
      - log: "Post /v2/user"
      - post:
          url: "/v2/user"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "properties.id"
            - hasProperty: "properties.username"
            - hasProperty: "properties.firstName"
            - hasProperty: "properties.lastName"
            - hasProperty: "properties.email"
            - hasProperty: "properties.phone"
            - hasProperty: "properties.password"
            - hasProperty: "properties.userStatus"
      - think: 1
