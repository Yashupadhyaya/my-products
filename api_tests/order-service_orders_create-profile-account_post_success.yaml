# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/create-profile-account_post for http method type POST in artillery framework
# RoostTestHash=44a4fe3677
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "order-service_orders_create-profile-account_post_success.csv"
    fields:
      - "channel"
      - "X-Trace-Id"
      - "userid"
      - "username"
      - "entityId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Create profile account"
    flow:
      - log: "Starting POST /order-service/orders/create-profile-account request"
      - post:
          url: "/order-service/orders/create-profile-account"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            userid: "{{ userid }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
          expect:
            - statusCode: 201
            - hasProperty: "orderId"
            - hasProperty: "requestId"
          capture:
            - json: "$.orderId"
              as: "orderId"
            - json: "$.requestId"
              as: "requestId"
      - think: 1
