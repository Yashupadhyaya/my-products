# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet_put for http method type PUT in artillery framework
# RoostTestHash=3c023e4a7b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_put_success.csv"
    fields: ["name", "photoUrls", "status"]
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: API Success Test Scenario
    flow:
      - log: "Starting test scenario for successful API responses..."
      - post:
          url: "/v2/pet"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
            - equals:
                status: "{{ status }}"
            - hasHeader: 'Content-Type'
      - think: 5
      - log: "Request was a success! Moving onto the next test..."
