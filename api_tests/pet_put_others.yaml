# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet_put for http method type PUT in artillery framework
# RoostTestHash=3c023e4a7b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "pet_put_others.csv"
    fields:
      - id
      - name
      - status
    delimiter: "^|^"
    skipHeader: true
  ensure:
    - statusCode "200|201|204|400|401|403|404|500"
  plugins:
    expect: {}
scenarios:
  - name: "Test non-success cases for /v2/pet endpoint"
    flow:
      - log: "Starting test with invalid payload (missing name)"
      - post:
          url: "/v2/pet"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Starting test with invalid payload (missing id)"
      - post:
          url: "/v2/pet"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Starting test with invalid auth token"
      - post:
          url: "/v2/pet"
          headers:
            api_key: "invalid_token"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Starting test with incorrect url"
      - post:
          url: "/v2/pet_invalid"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
