# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}/{form-instance-id}_post for http method type POST in artillery framework
# RoostTestHash=92502ea186
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_form-instance-id_post_success.csv"
    fields:
      - "form-instance-id"
      - "type"
      - "status"
      - "currentViewId"
      - "nextViewId"
      - "links"
      - "parentId"
    skipHeader: true
    delimiter: "^|^"
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    X-HSBC-Request-Correlation-Id: "{{$uuid}}"

scenarios:
  - name: "Successful API requests"
    flow:
      - log: "Starting request to /forms/{form-type}/{form-instance-id}"
      - post:
          url: "/forms/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "{{$uuid}}"
          json:
            form-instance-id: "{{ form-instance-id }}"
            type: "{{ type }}"
            status: "{{ status }}"
            currentViewId: "{{ currentViewId }}"
            nextViewId: "{{ nextViewId }}"
            links: "{{ links }}"
            parentId: "{{ parentId }}"
          expect:
            - statusCode: 200
            - hasProperty: "messages"
            - hasProperty: "currentViewId"
            - hasProperty: "nextViewId"
            - hasProperty: "views"
            - hasProperty: "count"
      - think: 1
