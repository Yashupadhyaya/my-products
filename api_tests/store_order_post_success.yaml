# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/order_post for http method type POST in artillery framework
# RoostTestHash=340dd2a1e1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "./store_order_post_success.csv"
    fields:
      - id
      - petId
      - quantity
      - shipDate
      - status
      - complete
    skipHeader: true
    delimiter: "^|^"
    
scenarios:
  - name: Artillery Test Scenario
    flow:
      - log: "Initiating POST request to /v2/store/order"
      - post:  
          url: "/v2/store/order"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
      - capture:
            - json: "$.message"
              as: "message"
      - expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "petId"
            - hasProperty: "quantity"
            - hasProperty: "shipDate"
            - hasProperty: "status"
            - hasProperty: "complete"
            - equals: 
              - "{{ id }}"
            - equals: 
              - "{{ petId }}"
            - equals: 
              - "{{ quantity }}"
            - equals: 
              - "{{ shipDate }}"
            - equals: 
              - "{{ status }}"
            - equals: 
              - "{{ complete }}"
      - think: 3
