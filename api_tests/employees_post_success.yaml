# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "employees_post_success.csv"
    fields:
      - "id"
      - "name"
      - "email"
      - "jobTitle"
    skipHeader: true
    delimiter: "^|^"
      
scenarios:
  - name: "Employee API test scenario"
    flow:
      - log: "Starting POST /employees request"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
      - think: 1
      - expect:
          - statusCode: 201
          - hasProperty: "id"
          - hasProperty: "name"
          - hasProperty: "email"
          - hasProperty: "jobTitle"
