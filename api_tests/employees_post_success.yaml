# ********RoostGPT********

# Test generated by RoostGPT for test api-spec-test-v1 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "http://example.com"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "employees_post_success.csv"
    fields:
      - "id"
      - "jobTitle"
      - "name"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Test Scenario - Create Employees"
    flow:
      - log: "Starting test for employee creation"
      - post:
          url: "/employees"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "jobTitle"
            - hasProperty: "name"
            - hasProperty: "email"
      - think: 1
