# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_post for http method type POST in artillery framework
# RoostTestHash=b517afeac4
# 
# 

# ********RoostGPT********
config:
  target: "http://api.example.com" # replace with your target API URL
  environments:
    production:
      target: "{{ $processEnvironment.nwcwiuwc_API_HOST }}"
      plugins:
        expect: {}
  payload:
    path: "employees_post_success.csv"
    fields: ["id", "jobTitle", "name", "email"]
    skipHeader: true
    delimiter: "^|^"
  http:
    timeout: 10
    pool: 10
  phases:
    - duration: 60
      arrivalRate: 5
      rampTo: 50
      name: "Warm up the application"
    - duration: 120
      arrivalRate: 50
      name: "Sustained max load"
scenarios:
  - name: "Create new Employee"
    flow:
      - log: "Adding employee"
      - post:
          url: "/employees"
          headers:
            content-type: "application/json"
            Authorization: "{{ $processEnvironment.nwcwiuwc_AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            email: "{{ email }}"
            jobTitle: "{{ jobTitle }}"
          expect:
            - statusCode: 201
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "jobTitle"
            - equals: ["name", "{{ name }}"]
            - equals: ["email", "{{ email }}"]
            - equals: ["jobTitle", "{{ jobTitle }}"]
      - think: 1
