# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/{username}_put for http method type PUT in artillery framework
# RoostTestHash=ec22222bac
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 2   
  plugins:
    expect: {}
  payload:
    path: 'user_username_put_success.csv'
    fields:
      - "User"
    skipHeader: true
    delimiter: '^|^'
  defaults:
    headers:
      api_key: '{{$processEnvironment.AUTH_TOKEN}}'
    
scenarios:
  - name: Scenario for User PUT endpoint for successful response testing
    flow:
        - log: "Executing PUT /v2/user/{username} request for changing user details" 
        - put: "/v2/user/{{ User }}" # put operation
          headers: # including required headers
            api_key: '{{$processEnvironment.AUTH_TOKEN}}'
          json: # request body
            id: 1
            username: "{{ User }}"
            firstName: "John"
            lastName: "Doe"
            email: "johndoe@email.com"
            password: "password123"
            phone: "1234567890"
            userStatus: 1
          expect: # the validation section for the request
            - statusCode: 200
            - hasProperty: 'id'
            - hasProperty: 'username'
            - hasProperty: 'firstName'
            - hasProperty: 'lastName'
            - hasProperty: 'email'
            - hasProperty: 'password'
            - hasProperty: 'phone'
            - hasProperty: 'userStatus'
            - equals:
                id: 1
                username: "{{ User }}"
                firstName: "John"
                lastName: "Doe"
                email: "johndoe@email.com"
                password: "password123"
                phone: "1234567890"
                userStatus: 1
        - think: 2
