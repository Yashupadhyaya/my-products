# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /.well-known/health_get for http method type GET in artillery framework
# RoostTestHash=b679c0374b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: ".well-known_health_get_success.csv"
    skipHeader: true
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "Health check"
    flow:
      - log: "Starting Health check scenario"
      - get:
          url: "/.well-known/health"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "83a37bff-b5a3-43b6-8628-7afb2565dbe7"
          expect:
            - statusCode: 200
            - hasProperty: "apiVersion"
            - hasProperty: "status"
            - hasProperty: "datetime"
            - hasProperty: "hostInstanceId"
      - think: 1
