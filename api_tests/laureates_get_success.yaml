# ********RoostGPT********

# Test generated by RoostGPT for test testingartillerytest using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /laureates_get for http method type GET in artillery framework
# RoostTestHash=ff433d7857
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_nobel_API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Validate Successful Responses for Laureates API"
    flow:
      # Scenario Description
      - log: "Starting Laureate API Success Test Scenario. Validating various success responses."
      
      # Test case: Retrieve all laureates with pagination
      - log: "Request: Retrieve laureates with pagination parameters offset=10 and limit=5."
      - get:
          url: "/2.1/laureates?offset={{ offset }}&limit={{ limit }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.url1_nobel_AUTH_TOKEN }}"
          expect:
            statusCode: 200
            contentType: "application/json"
            hasProperty:
              - "laureates"
              - "meta"
      - think:
          time: 2
      
      # Test case: Retrieve laureates based on gender and sort order
      - log: "Request: Retrieve laureates by gender=male, sorted alphabetically."
      - get:
          url: "/2.1/laureates?gender={{ gender }}&sort={{ sort }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.url1_nobel_AUTH_TOKEN }}"
          expect:
            statusCode: 200
            contentType: "application/json"
            hasProperty:
              - "laureates"
              - "meta"
      - think:
          time: 2

      # Test case: Using ID parameter
      - log: "Request: Retrieve laureate details using ID=456."
      - get:
          url: "/2.1/laureates?ID={{ ID }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.url1_nobel_AUTH_TOKEN }}"
          expect:
            statusCode: 200
            contentType: "application/json"
            hasProperty:
              - "laureates"
              - "meta"
      - think:
          time: 2

      # Test case: Retrieve laureates born on a specific date
      - log: "Request: Retrieve laureates born on birthDate=1989-04-25."
      - get:
          url: "/2.1/laureates?birthDate={{ birthDate }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.url1_nobel_AUTH_TOKEN }}"
          expect:
            statusCode: 200
            contentType: "application/json"
            hasProperty:
              - "laureates"
              - "meta"
      - think:
          time: 2

      # Test case: Search laureate by motivation text
      - log: "Request: Search laureates with motivation containing 'Peace'."
      - get:
          url: "/2.1/laureates?motivation={{ motivation }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.url1_nobel_AUTH_TOKEN }}"
          expect:
            statusCode: 200
            contentType: "application/json"
            hasProperty:
              - "laureates"
              - "meta"
      - think:
          time: 2
