# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}/{form-instance-id}_put for http method type PUT in artillery framework
# RoostTestHash=62e0da3209
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_form-instance-id_put_success.csv"
    fields:
      - "form-instance-id"
      - "type"
      - "status"
      - "currentViewId"
      - "nextViewId"
      - "links"
      - "parentId"
    skipHeader: true
    delimiter: "^|^"
    
scenarios:
  - name: "Validate successful PUT requests to /forms/{form-type}/{form-instance-id}"
    flow:
      - log: "Starting PUT request to /forms/{form-type}/{form-instance-id}"
      - put:
          url: "/forms/{{ type }}/{{ form-instance-id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "{{$uuid}}"
          json:
            form-instance-id: "{{ form-instance-id }}"
            type: "{{ type }}"
            status: "{{ status }}"
            currentViewId: "{{ currentViewId }}"
            nextViewId: "{{ nextViewId }}"
            links: "{{ links }}"
            parentId: "{{ parentId }}"
      - expect:
          - statusCode: 200
          - hasProperty: "status"
          - hasProperty: "messages"
          - hasProperty: "currentViewId"
          - hasProperty: "nextViewId"
          - hasProperty: "domainSessionId"
          - hasProperty: "views"
          - hasProperty: "count"
      - think: 1
