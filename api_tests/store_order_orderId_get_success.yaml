# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/order/{orderId}_get for http method type GET in artillery framework
# RoostTestHash=959486ed06
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_order_orderId_get_success.csv"
    fields:
        - "orderId"
    skipHeader: true
    delimiter: "^|^"
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        api_key: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: Success scenario for API response
    flow:
      - log: "Starting test for /v2/store/order/{orderId}"
      - think: 1
      - get:
          url: "/v2/store/order/{{ orderId }}"
          secure: false
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "petId"
          - hasProperty: "quantity"
          - hasProperty: "shipDate"
          - hasProperty: "status"
          - hasProperty: "complete"
      - equals:
          id: "{{ orderId }}"
      - log: "Finished test for API /v2/store/order/{orderId}"
