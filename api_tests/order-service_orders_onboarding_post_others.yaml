# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/onboarding_post for http method type POST in artillery framework
# RoostTestHash=e0f23024c5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "order-service_orders_onboarding_post_others.csv"
    fields:
      - "channel"
      - "X-Trace-Id"
      - "username"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - log: "Sending invalid POST request to /order-service/orders/onboarding"
      - post:
          url: "/order-service/orders/onboarding"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending POST request to /order-service/orders/onboarding without required headers"
      - post:
          url: "/order-service/orders/onboarding"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending POST request to incorrect URL"
      - post:
          url: "/invalid/url"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
