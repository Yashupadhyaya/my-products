# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/order/{orderId}_get for http method type GET in artillery framework
# RoostTestHash=959486ed06
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  ensure:
    p99: 200
  payload:
    path: "store_order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      api_key: "{{$processEnvironment.AUTH_TOKEN}}"
  plugins:
    expect: {}

scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Create order with invalid input (orderId < 1)"
      - post:
          url: "/v2/store/order/-1"
          json:
            id: -1
            petId: 1
            quantity: 1
            shipDate: "2020-09-08T16:04:28.824Z"
            status: "placed"
            complete: true
          expect:
            - statusCode: 400
      - think: 1
      - log: "Create order with invalid input (orderId > 10)"
      - post:
          url: "/v2/store/order/11"
          json:
            id: 11
            petId: 1
            quantity: 1
            shipDate: "2020-09-08T16:04:28.824Z"
            status: "placed"
            complete: true
          expect:
            - statusCode: 400
      - think: 1
      - log: "Find order by ID without authorization"
      - get:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
      - think: 1
      - log: "Find order by ID with incorrect authorization"
      - get:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "incorrect"
          expect:
            - statusCode: 403
      - think: 1
      - log: "Find order by ID with invalid orderId"
      - get:
          url: "/v2/store/order/invalid"
          expect:
            - statusCode: 405
      - think: 1
      - log: "Remove order by ID without authorization"
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
      - think: 1
      - log: "Remove order by ID with incorrect authorization"
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            api_key: "incorrect"
          expect:
            - statusCode: 403
      - think: 1
      - log: "Remove order by ID with invalid orderId"
      - delete:
          url: "/v2/store/order/invalid"
          expect:
            - statusCode: 405
      - think: 1
