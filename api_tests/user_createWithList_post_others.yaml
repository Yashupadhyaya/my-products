# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/createWithList_post for http method type POST in artillery framework
# RoostTestHash=41ad79e90d
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_createWithList_post_others.csv"
    fields:
      - "id"
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      Authorization: "Bearer {{ processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: Test scenario for non-successful API requests
    flow:
      - log: "Starting scenario for invalid payloads"
      - think: 1
      - post:
          url: "/v2/user/createWithList"
          json: 
            id: "invalid"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect: 
            - statusCode: 400
      - log: "Starting scenario for invalid authentication"
      - think: 1
      - delete:
          headers:
            Authorization: "InvalidToken"
          url: "/v2/user/{{ username }}"
          expect: 
            - statusCode: 401
      - log: "Starting scenario for unprocessable request"
      - think: 1
      - put:
          url: "/v2/user/{{ username }}"
          json: 
            email: "invalid"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect: 
            - statusCode: 422
      - log: "Starting scenario for invalid URL"
      - think: 1
      - get:
          url: "/v2/user/invalid_url"
          expect: 
            - statusCode: 404
