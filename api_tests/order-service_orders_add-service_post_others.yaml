# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/add-service_post for http method type POST in artillery framework
# RoostTestHash=9fd8e0bea4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order-service_orders_add-service_post_others.csv"
    fields:
      - "channel"
      - "X-Trace-Id"
      - "username"
      - "entityId"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "https://dev-api.example.com"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "POST /order-service/orders/add-service"
    flow:
      - log: "Starting POST /order-service/orders/add-service request"
      - post:
          url: "/order-service/orders/add-service"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Invalid request"
      - think: 1
      - log: "Starting POST /order-service/orders/add-service with incorrect URL"
      - post:
          url: "/order-service/orders/add-service-invalid"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
