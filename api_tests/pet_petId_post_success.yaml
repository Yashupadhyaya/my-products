# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/{petId}_post for http method type POST in artillery framework
# RoostTestHash=c0bc5294a0
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
      name: "Warm up"
    - duration: 120
      arrivalRate: 5
      rampTo: 50
      name: "Ramp up load"
    - duration: 600
      arrivalRate: 50
      name: "Sustained load"
  payload:
    path: "pet_petId_post_success.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test successful API requests"
    flow:
      - log: "Starting test scenario for successful API requests"
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "status"
      - think: 1
      - log: "Completed test scenario for successful API requests"
