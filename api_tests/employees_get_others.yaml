# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "employees_get_others.csv"
    fields:
      - "employeeId"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending a GET request with invalid URL"
      - get:
          url: "/invalidURL"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a GET request without authorization token"
      - get:
          url: "/employees"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a GET request with invalid employeeId"
      - get:
          url: "/employees/{{ employeeId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a GET request with unauthorized employeeId"
      - get:
          url: "/employees/{{ employeeId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
