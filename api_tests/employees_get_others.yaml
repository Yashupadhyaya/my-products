# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.wcvjwcvwcw_API_HOST }}"
  ensure:
    p95: 2000
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "employees_get_others.csv"
    fields:
      - "fieldname1"
      - "fieldname2"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Attempting a GET request with invalid URL"
      - get:
          url: "/invalidURL"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 5

      - log: "Sending a GET request without authentication token"
      - get:
          url: "/employees"
          expect:
               - statusCode: 401
               - hasProperty: "error"
      - think: 5

      - log: "Sending a GET request with invalid authentication token"
      - get:
          url: "/employees"
          headers:
            AUTH_TOKEN: InvalidToken
          expect:
               - statusCode: 403
               - hasProperty: "error"
      - think: 5

      - log: "Sending a GET request with malformed query params"
      - get:
          url: "/employees?searchString=123"
          headers:
            AUTH_TOKEN: {{$processEnvironment.wcvjwcvwcw_AUTH_TOKEN}}
          expect:
               - statusCode: 400
               - hasProperty: "error"
      - think: 5

      - log: "Sending a GET request with non-existent record"
      - get:
          url: "/employees?searchString=nonexistent"
          headers:
            AUTH_TOKEN: {{$processEnvironment.wcvjwcvwcw_AUTH_TOKEN}}
          expect:
               - statusCode: 404
               - hasProperty: "error"
      - think: 5
