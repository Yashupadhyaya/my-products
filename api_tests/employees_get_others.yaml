# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payloads:
    - path: "employees_get_others.csv"
      fields:
        - "name"
        - "email"
      skipHeader: true
      delimiter: "^|^"
scenarios:
  - name: Unsuccessful API Requests
    flow:
      - log: "Invalid URL request."
      - get:
          url: "/emps"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Unauthorized request."
      - get:
          url: "/employees"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasHeader: "WWW-Authenticate"
      - think: 1
      - log: "Incorrect payload request."
      - post:
          url: "/employees"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            name: 1234567890
            email: "invalid email"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Feature not implemented."
      - patch:
          url: "/employees/{{ name }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 501
      - think: 1
      - log: "Trying to delete non-existing resource."
      - delete:
          url: "/employees/non_existing_user"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
