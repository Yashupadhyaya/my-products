# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "employees_get_others.csv"
    fields:
      - "employeeId"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "First, let's try an invalid URL"
      - get:
          url: "/invalid-url"
          expect:
            - statusCode: 404
      - think: 2

      - log: "Now, let's try a valid URL but without required authentication"
      - get:
          url: "/employees"
          expect:
            - statusCode: 401
      - think: 2

      - log: "Now, let's try a valid URL with authentication but invalid payload"
      - post:
          url: "/employees"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidField: "invalidValue"
          expect:
            - statusCode: 400
      - think: 2

      - log: "Finally, let's try a valid URL with authentication and valid payload but non-existing resource"
      - delete:
          url: "/employees/{{ employeeId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
