# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=36caaf0283
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  variables:
    auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: Test non-success API responses
    flow:
      - log: "Sending wrong orderId to check the response"
      - put:
          url: "/v2/store/order/ab123"
          headers:
            api_key: "{{ auth_token }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending a DELETE request without auth"
      - delete:
          url: "/v2/store/order/100"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a DELETE request with auth but for a non-existent resource"
      - delete:
          url: "/v2/store/order/999999999999"
          headers:
            api_key: "{{ auth_token }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a malformed DELETE request with auth and invalid URL"
      - delete:
          url: "/v2/store/order/100/extra_path"
          headers:
            api_key: "{{ auth_token }}"
          expect:
            - statusCode: 404
      - think: 1
