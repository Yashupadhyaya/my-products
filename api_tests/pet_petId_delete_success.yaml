# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=1138243cb1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_success.csv"
    fields: ["petId"]
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: Test successful DELETE /v2/pet/{petId} request
    flow:
      - log: "Starting test for DELETE /v2/pet/{petId} request"
      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$"
            as: "response"
      - expect:
          statusCode: 200
          hasProperty: ["petId", "name"]
      - think: 5
