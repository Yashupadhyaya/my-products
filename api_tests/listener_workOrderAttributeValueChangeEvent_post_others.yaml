# ********RoostGPT********

# Test generated by RoostGPT for test postman-testvtal using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /listener/workOrderAttributeValueChangeEvent_post for http method type POST in artillery framework
# RoostTestHash=9481d96e5e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  plugins:
    expect: {}
  payload:
    path: "listener_workOrderAttributeValueChangeEvent_post_others.csv"
    fields:
      - "id"
      - "name"
      - "timeOccurred"
      - "timeZoneOccurred"
      - "workOrderId"
      - "lifeCycleStatus"
      - "associatedDocument"
      - "businessUnit"
      - "operationalPriority"
      - "readyForTechnicalDispatch"
      - "activityType"
      - "category"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Send a POST request with invalid payload data"
      - post:
          url: "/listener/workOrderAttributeValueChangeEvent"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            notification:
              event:
                id: "{{ id }}"
                name: "{{ name }}"
                timeOccurred: "{{ timeOccurred }}"
                timeZoneOccurred: "{{ timeZoneOccurred }}"
            workOrder:
              id: "{{ workOrderId }}"
              lifeCycleStatus: "{{ lifeCycleStatus }}"
              associatedDocument: "{{ associatedDocument }}"
              businessUnit: "{{ businessUnit }}"
              operationalPriority: "{{ operationalPriority }}"
              readyForTechnicalDispatch: "{{ readyForTechnicalDispatch }}"
              task:
                activityType: "{{ activityType }}"
                category: "{{ category }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
      - think: 1
      - log: "Send a POST request with missing Authorization header"
      - post:
          url: "/listener/workOrderAttributeValueChangeEvent"
          headers:
            Content-Type: "application/json"
          json:
            notification:
              event:
                id: "{{ id }}"
                name: "{{ name }}"
                timeOccurred: "{{ timeOccurred }}"
                timeZoneOccurred: "{{ timeZoneOccurred }}"
            workOrder:
              id: "{{ workOrderId }}"
              lifeCycleStatus: "{{ lifeCycleStatus }}"
              associatedDocument: "{{ associatedDocument }}"
              businessUnit: "{{ businessUnit }}"
              operationalPriority: "{{ operationalPriority }}"
              readyForTechnicalDispatch: "{{ readyForTechnicalDispatch }}"
              task:
                activityType: "{{ activityType }}"
                category: "{{ category }}"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
      - think: 1
      - log: "Send a POST request to an incorrect URL"
      - post:
          url: "/listener/workOrderAttributeValueChangeEvent/invalid"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            notification:
              event:
                id: "{{ id }}"
                name: "{{ name }}"
                timeOccurred: "{{ timeOccurred }}"
                timeZoneOccurred: "{{ timeZoneOccurred }}"
            workOrder:
              id: "{{ workOrderId }}"
              lifeCycleStatus: "{{ lifeCycleStatus }}"
              associatedDocument: "{{ associatedDocument }}"
              businessUnit: "{{ businessUnit }}"
              operationalPriority: "{{ operationalPriority }}"
              readyForTechnicalDispatch: "{{ readyForTechnicalDispatch }}"
              task:
                activityType: "{{ activityType }}"
                category: "{{ category }}"
          expect:
            - statusCode: 404
            - hasProperty: "errors"
      - think: 1
