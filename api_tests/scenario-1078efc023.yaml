# Test generated by RoostGPT for test testing-integration-test using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "scenario-1078efc023.csv"
    fields:
      - "authorizationID"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Delete authorization"
    flow:
      - log: "Running scenario: Delete authorization"
      - delete:
          url: "/authorizations/{{ authorizationID }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
          capture:
            - json: "$.message"
              as: "deleteMessage"
      - think: 1
      - log: "Deleted authorization with ID: {{ authorizationID }}"
      - get:
          url: "/authorizations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "authorizations"
            - hasProperty: "total"
            - hasProperty: "authorizations.0"
            - hasProperty: "authorizations.0.id"
            - hasProperty: "authorizations.0.name"
            - hasProperty: "authorizations.0.email"
            - hasProperty: "authorizations.0.createdAt"
            - hasProperty: "authorizations.0.updatedAt"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - equals:
                - "authorizations.{{ authorizationID }}"
                - undefined
      - think: 1
      - log: "Authorization with ID: {{ authorizationID }} does not appear in GET request for /authorizations"
