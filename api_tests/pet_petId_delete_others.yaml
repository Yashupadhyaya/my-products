# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=1138243cb1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-successful API requests test scenario"
    flow:
      - log: "Step 1: Sending DELETE request to URL with incorrect petId"
      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
          - hasProperty: "message"
      - think: 1
      - log: "Step 2: Sending DELETE request to incorrect URL"
      - delete:
          url: "/v2/pets/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
          - hasProperty: "message"
      - think: 1
      - log: "Step 3: Sending DELETE request without an authorization header"
      - delete:
          url: "/v2/pet/{{ petId }}"
      - expect:
          - statusCode: 401
          - hasProperty: "message"
      - think: 1 
