# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}/{form-instance-id}_post for http method type POST in artillery framework
# RoostTestHash=92502ea186
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_form-instance-id_post_others.csv"
    fields: 
      - "formType"
      - "formInstanceId"
    skipHeader: true
    delimiter: "^|^"
  
scenarios:
  - name: "Testing non-successful API responses"
    flow:
      - log: "Sending invalid POST request to /forms/{form-type}/{form-instance-id}"
      - post:
          url: "/forms/{{ formType }}/{{ formInstanceId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "invalid-id"
          json:
            form-instance-id: "invalid-id"
            type: "invalid-type"
            status: "invalid-status"
            currentViewId: "invalid-id"
            nextViewId: "invalid-id"
            links: {}
            parentId: "invalid-id"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Sending POST request to invalid URL"
      - post:
          url: "/invalid-url/{{ formType }}/{{ formInstanceId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "{{ formInstanceId }}"
          json:
            form-instance-id: "{{ formInstanceId }}"
            type: "{{ formType }}"
            status: "invalid-status"
            currentViewId: "invalid-id"
            nextViewId: "invalid-id"
            links: {}
            parentId: "invalid-id"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Sending POST request without authorization token"
      - post:
          url: "/forms/{{ formType }}/{{ formInstanceId }}"
          headers:
            X-HSBC-Request-Correlation-Id: "{{ formInstanceId }}"
          json:
            form-instance-id: "{{ formInstanceId }}"
            type: "{{ formType }}"
            status: "invalid-status"
            currentViewId: "invalid-id"
            nextViewId: "invalid-id"
            links: {}
            parentId: "invalid-id"
          expect:
            - statusCode: 401
      - think: 1
