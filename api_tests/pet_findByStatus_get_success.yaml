# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=44cafec54d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_findByStatus_get_success.csv"
    fields:
      - "id"
      - "name"
      - "status"
    skipHeader: true
    delimiter: "^|^"
  ensure:
    - statusCode: 200
    - hasProperty: "id"
    - hasProperty: "name"
    - hasProperty: "status"
scenarios:
  - name: "Find Pets by status"
    flow:
      - log: "Starting test for /v2/pet/findByStatus API endpoint"
      - get: 
          url: "/v2/pet/findByStatus"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          qs:
            status: 
              - "available"
              - "pending"
              - "sold"
          expect: 
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "status"
      - think: 1
