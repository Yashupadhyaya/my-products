# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet_post for http method type POST in artillery framework
# RoostTestHash=33f954929c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_post_others.csv"
    fields:
      - "id"
      - "category"
      - "name"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      auth_token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Post Pet Scenario"
    flow:
      - log: "POST /v2/pet with incorrect data"
      - post:
          url: "/v2/pet"
          headers:
            Content-Type: "application/json"
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "type"
            - hasProperty: "title"
            - hasProperty: "status"
            - hasProperty: "detail"
            - hasProperty: "instance"
            - hasProperty: "additionalProp1"
      - think: 1
