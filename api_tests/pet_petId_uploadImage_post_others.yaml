# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework
# RoostTestHash=8dac89cb33
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_uploadImage_post_others.csv"
    fields: ["petId", "additionalMetadata", "file"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Testing various failure-inducing requests on a single API endpoint"
    flow:

      - log: "Sending a POST request with invalid petId"
      - post: 
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            content-type: "multipart/form-data"
            petstore_auth: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
      - expect:
          - statusCode: 404
      - think: 1
      
      - log: "Sending a POST request with missing file"
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            content-type: "multipart/form-data"
            petstore_auth: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
      - expect:
          - statusCode: 400
      - think: 1
      
      - log: "Sending a POST request to a wrong URL"
      - post:
          url: "/v2/pet/incorrect/url"
          headers:
            content-type: "multipart/form-data"
            petstore_auth: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            petId: "{{ petId }}"
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
      - expect:
          - statusCode: 404
      - think: 1
