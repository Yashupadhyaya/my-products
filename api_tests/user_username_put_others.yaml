# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/{username}_put for http method type PUT in artillery framework
# RoostTestHash=ec22222bac
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    dev:
      target: "http://dev.api.host"
      http:
        timeout: 10
  payload:
    path: "user_username_put_others.csv"
    fields:
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios: 
  - name: "Test failed attempts to update user"
    flow:
      - log: "Fail to update user because of bad request"
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}" 
          expect:
            - statusCode: 400
      - think: 5

      - log: "Fail to update user because of not authorized"
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            Content-Type: "application/json"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}" 
          expect:
            - statusCode: 401
      - think: 5

      - log: "Fail to update user because of not found"
      - put:
          url: "/v2/user/nonexistentUser"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}" 
          expect:
            - statusCode: 404
      - think: 5

      - log: "Fail to update user because of invalid url"
      - put:
          url: "/v2/user/{{ username }}/invalidUrl"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}" 
          expect:
            - statusCode: 400
      - think: 5
