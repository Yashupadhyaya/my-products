# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework
# RoostTestHash=8dac89cb33
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Check successful API responses"
    flow:
      - log: "Executing a successful request to /v2/pet/{petId}/uploadImage"
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "Test metadata"
            file: "./test-image.jpg"
          expect:
            - statusCode: 200
      - hasProperty: "code"
      - hasProperty: "type"
      - hasProperty: "message"
      - think: 5
