# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=9124f669d9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "pet_findByTags_get_others.csv"
    fields:
      - "name"
      - "key"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending incorrect POST request"
      - post:
          url: "/v2/pet/findByTags?tag={{ name }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ key }}"
            name: "{{ name }}"
          expect:
            - statusCode: 404
            
      - think: 5

      - log: "Sending GET request with incorrect authentication"
      - get:
          url: "/v2/pet/findByTags?tag={{ name }}"
          headers:
            api_key: "Incorrect_Token"
          expect:
            - statusCode: 401

      - think: 5
