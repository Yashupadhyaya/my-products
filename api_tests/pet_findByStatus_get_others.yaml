# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=44cafec54d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        rejectUnauthorized: false
  payload:
    path: "pet_findByStatus_get_others.csv"
    fields:
      - "..."
     
scenarios:
  - name: "Tests fail responses for /v2/pet/findByStatus endpoint"
    flow:
     - log: "Sending a GET request with no queryString parameters to /v2/pet/findByStatus"
     - get:
         url: "/v2/pet/findByStatus"
         headers:
            "api_key": "{{ $processEnvironment.AUTH_TOKEN }}"
     - expect:
            statusCode: 400
     - think: 1

     - log: "Sending a GET request with invalid status parameter to /v2/pet/findByStatus endpoint"
     - get:
         url: "/v2/pet/findByStatus?status=invalidStatus"
         headers:
            "api_key": "{{ $processEnvironment.AUTH_TOKEN }}"
     - expect:
            statusCode: 400
     - think: 1

     - log: "Sending a GET request to an invalid URL"
     - get:
         url: "/random/invalid/url"
         headers:
            "api_key": "{{ $processEnvironment.AUTH_TOKEN }}"
     - expect:
            statusCode: 404
     - think: 1
