# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/{username}_delete for http method type DELETE in artillery framework
# RoostTestHash=5caa5e3efb
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_others.csv"
    fields:
      - "username"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-success API response test"
    flow:
      - log: "Testing Delete User with Invalid Username"
      - delete:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing Delete User without Authorization"
      - delete:
          url: "/v2/user/testuser"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing Delete User with Incorrect URL"
      - delete:
          url: "/v2/usar/{{ username }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
