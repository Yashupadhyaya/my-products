# Test generated by RoostGPT for test testing-integration-test using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "scenario-70b6f04041.csv"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Grant authorization"
    flow:
      # Create a new account
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "{{ commitHash }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "api-version"
            - hasProperty: "commit-hash"
            - headerEquals:
                api-version: "1.0.0"
            - matchesRegexp:
                commit-hash: "[0-9a-f]{7}"
          capture:
            json: "$.accountId"
            as: "accountId"
      - think: 1
      # Grant authorization to the new account
      - post:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "{{ commitHash }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "api-version"
            - hasProperty: "location"
            - headerEquals:
                api-version: "1.0.0"
          capture:
            json: "$.authorizationId"
            as: "authorizationId"
      - think: 1
      # Confirm access to the new account
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "{{ commitHash }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "authorizations"
            - hasProperty: "authorizations.0"
            - hasProperty: "authorizations.0.id"
            - hasProperty: "authorizations.0.name"
            - hasProperty: "authorizations.0.email"
            - hasProperty: "authorizations.0.createdAt"
            - hasProperty: "authorizations.0.updatedAt"
            - hasProperty: "api-version"
            - headerEquals:
                api-version: "1.0.0"
            - equals:
                authorizations[0].id: "{{ authorizationId }}"
