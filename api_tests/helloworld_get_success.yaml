# ********RoostGPT********

# Test generated by RoostGPT for test test-raml-config using AI Type  and AI Model 
# 
# Test generated for /helloworld_get for http method type GET in artillery framework
# RoostTestHash=e5dd99f446
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "helloworld_get_success.csv"
    fields:
      - "message"
    skipHeader: true
    delimiter: "^|^"
  http:
    pool: 10
    timeout: 20
  environments:
    development:
      target: "http://localhost:3000"
    production:
      target: "http://api.example.com"

scenarios:
  - name: "Scenario 1: Test successful API requests"
    flow:
      - log: "Starting request for /helloworld API endpoint"
      - get:
          url: "/helloworld"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.message"
              as: "message"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "message"
      - equals:
          - "message"
          - "{{ message }}"
      - think: 1
