# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}/{form-instance-id}_put for http method type PUT in artillery framework
# RoostTestHash=62e0da3209
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "forms_form-type_form-instance-id_put_others.csv"
    fields:
      - "form-type"
      - "form-instance-id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      - log: "Sending invalid PUT request to /forms/{form-type}/{form-instance-id}"
      - put:
          url: "/forms/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "invalid-id"
          json:
            form-instance-id: "invalid-id"
            type: "invalid-type"
            status: "invalid-status"
            currentViewId: "invalid-id"
            nextViewId: "invalid-id"
            links: {}
            parentId: "invalid-id"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "errors"
      - think: 1
      - log: "Sending PUT request to incorrect URL"
      - put:
          url: "/incorrect/url/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "invalid-id"
          json:
            form-instance-id: "invalid-id"
            type: "invalid-type"
            status: "invalid-status"
            currentViewId: "invalid-id"
            nextViewId: "invalid-id"
            links: {}
            parentId: "invalid-id"
          expect:
            - statusCode: 404
      - think: 1
