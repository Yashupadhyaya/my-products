# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/logout_get for http method type GET in artillery framework
# RoostTestHash=696422fb14
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_logout_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Logout User Invalid Flow Test"
    flow:
      - log: "Starting logout user flow"
      - post:
          url: "/v2/user/logout/"
          headers:
            Content-Type: "application/json"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
      - think: 1
      - capture:
          json: "$.message"
          as: "logoutMessage"
      - expect:
          statusCode: 400
      - log: "Test run finished for logout user flow"
