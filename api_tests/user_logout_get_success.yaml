# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/logout_get for http method type GET in artillery framework
# RoostTestHash=696422fb14
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_logout_get_success.csv"
    fields:
      - "username"
    skipHeader: true
    delimiter: "^|^"
  environments:
    test:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "User logout success scenario"
    flow:
      - log: "Starting user logout scenario"
      - think:
         duration: 1
      - get:
          url: "/v2/user/logout"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - contentType: "json"
            - hasProperty: "message"
            - equals: 
                message: "User Logout Success"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
      - log: "User logout scenario successfully performed"
      - think: 
          duration: 1
