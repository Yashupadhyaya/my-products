# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.vwvwvwv_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "employees_id_get_success.csv"
    fields:
      - "id"
    delimiter: "^|^"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Get Employee by ID Success Scenario"
    flow:
      - log: "Starting test for GET /employees/{id} endpoint"
      - get:
          url: "/employees/{{ id }}"
          headers:
            {{ name }}: "{{ $processEnvironment.vwvwvwv_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
          expect:
            - hasProperty: "name"
          expect:
            - hasProperty: "jobTitle"
          expect:
            - hasProperty: "email"
          expect:
            - equals: ["headers.content-type", "application/json"]
      - think: 1
