# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "employees_id_get_success.csv"
    fields: ["id"]
    delimiter: "^|^"
    skipHeader: true
  environments:
    development:
    target: "{{ $processEnvironment.API_HOST }}"
    http:
      timeout: 10

scenarios:
  - name: "Validate successful API Requests"
    flow:
      - log: "Starting test scenario for /employees/{id} endpoint"
      - get:
        url: "/employees/{{ id }}"
        headers:
          Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "jobTitle"
          - hasProperty: "name"
          - hasProperty: "email"
      - think: 1
