# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "$(API_HOST)"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_success.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    local:
      target: "$(API_HOST)"
      headers:
        Authorization: "Bearer $(AUTH_TOKEN)"
scenarios:
  - name: "Test successful API responses"
    flow:
      - log: "Starting scenario for GET /employees/{id}"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer $(AUTH_TOKEN)"
          expect:
            - statusCode: 200
            - hasProperty: "email"
            - hasProperty: "jobTitle"
            - hasProperty: "name"
      - think: 1
