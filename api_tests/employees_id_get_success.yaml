# ********RoostGPT********

# Test generated by RoostGPT for test api-spec-test-v1 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "http://localhost:3000"
  environments:
    - name: "development"
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_success.csv"
    fields: ["id"]
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: "Testing successful API responses"
    flow:
      - log: "Starting test for GET /employees/{id}"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "jobTitle"
      - think: 1
