# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.dqdbqwidbwibwc_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_success.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    local:
      target: "{{ $processEnvironment.dqdbqwidbwibwc_API_HOST }}"
      headers:
        Authorization: "Bearer {{$processEnvironment.dqdbqwidbwibwc_AUTH_TOKEN}}"

scenarios:
  - name: "Test successful API responses"
    flow:
      - log: "Starting scenario for GET /employees/{id}"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.dqdbqwidbwibwc_AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "jobTitle"
            - hasProperty: "name"
            - hasProperty: "email"
      - think: 1
