# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}_get for http method type GET in artillery framework
# RoostTestHash=4bb8b3cdab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_get_success.csv"
    fields:
      - "formType"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test Successful Responses from /forms/{form-type} API"
    flow:
      - log: "Starting test for GET /forms/{form-type}"
      - get:
          url: "/forms/{{ formType }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "a1b2c3d4-e5f6-7a8b-9c0d-1e2f3a4b5c6d"
      - expect:
          - statusCode: 200
          - hasProperty: "status"
          - hasProperty: "messages"
          - hasProperty: "messages.information"
          - hasProperty: "messages.warning"
          - hasProperty: "messages.error"
          - hasProperty: "messages.generalError"
          - hasProperty: "messages.questionsError"
          - hasProperty: "currentViewId"
          - hasProperty: "nextViewId"
          - hasProperty: "domainSessionId"
          - hasProperty: "views"
          - hasProperty: "count"
          - hasProperty: "cause"
      - think: 1
