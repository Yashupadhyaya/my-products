# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}/{form-instance-id}_get for http method type GET in artillery framework
# RoostTestHash=75dc581068
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_form-instance-id_get_others.csv"
    fields: ["form-type", "form-instance-id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.com"
    prod:
      target: "http://prod.api.com"

scenarios:
  - name: "Test scenario for non-success API responses"
    flow:
      - log: "Starting test for GET request to /forms/{form-type}/{form-instance-id} with invalid form-type"
      - get:
          url: "/forms/invalid-form-type/{{ form-instance-id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "uuid"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Starting test for GET request to /forms/{form-type}/{form-instance-id} with invalid form-instance-id"
      - get:
          url: "/forms/{{ form-type }}/invalid-form-instance-id"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "uuid"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Starting test for GET request to /forms/{form-type}/{form-instance-id} with invalid URL"
      - get:
          url: "/invalid-url/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "uuid"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Starting test for GET request to /forms/{form-type}/{form-instance-id} with missing Authorization header"
      - get:
          url: "/forms/{{ form-type }}/{{ form-instance-id }}"
          headers:
            X-HSBC-Request-Correlation-Id: "uuid"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Starting test for GET request to /forms/{form-type}/{form-instance-id} with missing X-HSBC-Request-Correlation-Id header"
      - get:
          url: "/forms/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Starting test for GET request to /forms/{form-type}/{form-instance-id} with invalid Authorization token"
      - get:
          url: "/forms/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "Bearer invalid-token"
            X-HSBC-Request-Correlation-Id: "uuid"
          expect:
            - statusCode: 401
      - think: 1
