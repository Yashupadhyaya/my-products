# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=d905dac95c
# 
# 

# ********RoostGPT********
config:
  target: "{{ API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_login_get_success.csv"
    fields: ["username", "password"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ API_HOST }}"
      headers:
        Content-Type: "application/json"
        api_key: "{{ AUTH_TOKEN }}"
    prod:
      target: "{{ API_HOST }}"
      headers:
        Content-Type: "application/json"
        api_key: "{{ AUTH_TOKEN }}"
  
scenarios:
  - name: Successful API Requests
    flow:
      - log: "Starting GET request to user login"
      - get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          headers:
            api_key: "{{ AUTH_TOKEN }}"
      - expect:
          statusCode: 200
          hasHeader: "X-Expires-After"
          hasHeader: "X-Rate-Limit"
          hasProperty: "headers.X-Expires-After"
          hasProperty: "headers.X-Rate-Limit"
          equals:
            headers.X-Expires-After.type: "string"
            headers.X-Rate-Limit.type: "integer" 
      - log: "Completed GET request to user login"
      - think: 1
