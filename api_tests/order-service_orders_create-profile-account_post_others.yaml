# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/create-profile-account_post for http method type POST in artillery framework
# RoostTestHash=44a4fe3677
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order-service_orders_create-profile-account_post_others.csv"
    skipHeader: true
    fields:
      - "channel"
      - "X-Trace-Id"
      - "userid"
      - "username"
      - "entityId"
    delimiter: "^|^"

scenarios:
  - name: "Test scenario for non-successful API responses"
    flow:
      - log: "Sending invalid POST request to /order-service/orders/create-profile-account"
      - post:
          url: "/order-service/orders/invalid-endpoint"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            userid: "{{ userid }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending POST request to /order-service/orders/create-profile-account without authentication"
      - post:
          url: "/order-service/orders/create-profile-account"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            userid: "{{ userid }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
          json:
            key: "value"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending POST request to /order-service/orders/create-profile-account with invalid payload"
      - post:
          url: "/order-service/orders/create-profile-account"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            userid: "{{ userid }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400
      - think: 1
