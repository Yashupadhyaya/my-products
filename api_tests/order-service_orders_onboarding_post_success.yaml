# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/onboarding_post for http method type POST in artillery framework
# RoostTestHash=e0f23024c5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order-service_orders_onboarding_post_success.csv"
    fields:
      - "channel"
      - "X-Trace-Id"
      - "username"
    skipHeader: true
    delimiter: "^|^"
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Order Service Onboarding API Test"
    flow:
      - log: "Starting Order Service Onboarding API Test"
      - post:
          url: "/order-service/orders/onboarding"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 201
            - hasProperty: "orderId"
            - hasProperty: "requestId"
      - think: 1
