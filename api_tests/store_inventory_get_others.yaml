# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/inventory_get for http method type GET in artillery framework
# RoostTestHash=a66f7079af
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_inventory_get_others.csv"
    fields:
      - "invalidApiKey"
    skipHeader: true
    delimiter: "^|^"
  environments:
    variable:
      AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: Validate non-success responses for Get Inventory API
    flow:
      - log: "Performing GET request with Invalid API_KEY"
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{ invalidApiKey }}"
          capture:
            - json: "$"
              as: "invalidResponse"
          expect:
            - statusCode: 403  
      - think: 1
      - log: "Performing GET request with valid API_KEY but invalid URL"
      - get:
          url: "/v2/store/invalid_inventory"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$"
              as: "incorrectURLResponse"
          expect:
            - statusCode: 404
      - think: 1
