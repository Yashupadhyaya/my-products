# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/add-service_post for http method type POST in artillery framework
# RoostTestHash=9fd8e0bea4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "order-service_orders_add-service_post_success.csv"
    fields:
      - "channel"
      - "X-Trace-Id"
      - "username"
      - "entityId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Test Scenario for /order-service/orders/add-service"
    flow:
      - log: "Starting test request for /order-service/orders/add-service"
      - post:
          url: "/order-service/orders/add-service"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
            entityId: "{{ entityId }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
            - hasHeader: "Content-Type"
      - think: 1
