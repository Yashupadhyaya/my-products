# ********RoostGPT********

# Test generated by RoostGPT for test test-raml-config using AI Type  and AI Model 
# 
# Test generated for /helloworld_get for http method type GET in artillery framework
# RoostTestHash=e5dd99f446
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "helloworld_get_others.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test scenario for non-successful API responses"
    flow:
      - log: "Sending GET request with invalid URL"
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 404

      - think: 5

      - log: "Sending GET request without auth token"
      - get:
          url: "/helloworld"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401

      - think: 5

      - log: "Sending GET request with invalid auth token"
      - get:
          url: "/helloworld"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 403

      - think: 5

      - log: "Sending GET request with valid auth token but incorrect payload"
      - get:
          url: "/helloworld"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "invalid"
            field2: "invalid"
          expect:
            - statusCode: 400
            - hasProperty: "errorMessage"

      - think: 5
