# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}/{form-instance-id}_get for http method type GET in artillery framework
# RoostTestHash=75dc581068
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_form-instance-id_get_success.csv"
    fields:
      - "form-type"
      - "form-instance-id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "API Success Responses"
    flow:
      - log: "Starting test for GET /forms/{form-type}/{form-instance-id}"
      - get:
          url: "/forms/{{ form-type }}/{{ form-instance-id }}"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "{{$uuid}}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasProperty: "messages"
            - hasProperty: "messages.information"
            - hasProperty: "messages.warning"
            - hasProperty: "messages.error"
            - hasProperty: "messages.generalError"
            - hasProperty: "messages.questionsError"
            - hasProperty: "currentViewId"
            - hasProperty: "nextViewId"
            - hasProperty: "domainSessionId"
            - hasProperty: "views"
            - hasProperty: "count"
            - hasProperty: "cause"
      - think: 1
