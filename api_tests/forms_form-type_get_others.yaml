# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /forms/{form-type}_get for http method type GET in artillery framework
# RoostTestHash=4bb8b3cdab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "forms_form-type_get_others.csv"
    fields:
      - "form-type"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10

scenarios:
  - name: "Non-successful API requests"
    flow:
      
      - log: "Sending invalid GET request to /forms/{form-type}"
      
      - get:
          url: "/forms/{{ form-type }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "invalid-id"
          
      - expect:
          - statusCode: 401
      
      - think: 1

      - log: "Sending GET request to /forms/{form-type} without authorization header"
      
      - get:
          url: "/forms/{{ form-type }}"
          headers:
            X-HSBC-Request-Correlation-Id: "invalid-id"
          
      - expect:
          - statusCode: 403
      
      - think: 1

      - log: "Sending GET request to incorrect URL /forms/invalid-type"
      
      - get:
          url: "/forms/invalid-type"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            X-HSBC-Request-Correlation-Id: "invalid-id"
          
      - expect:
          - statusCode: 404
      
      - think: 1
