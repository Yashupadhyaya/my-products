# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/createWithArray_post for http method type POST in artillery framework
# RoostTestHash=2fceff8367
# 
# 

# ********RoostGPT********
config:
  target: "Change this to your API_HOST"
  
  phases:
    - duration: 60
      arrivalRate: 20
      
  plugins:
    expect: {}

  payload:
    path: "user_createWithArray_post_success.csv"
    fields: ["id", "username", "firstName", "lastName", "email", "password", "phone", "userStatus"]
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: Validation of success API responses
    flow:
      - log: "Executing POST request to create users with an array"
      - post:
          url: "/v2/user/createWithArray"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          headers:
            api_key: "Change this to your AUTH_TOKEN"
          expect:
            - statusCode: 200
            - hasProperty: "id"
      - think: 1
      - log: "Executing GET request to get the created user"
      - get:
          url: "/v2/user/{{ username }}"
          headers:
            api_key: "Change this to your AUTH_TOKEN"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "username"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "email"
            - hasProperty: "password"
            - hasProperty: "phone"
            - hasProperty: "userStatus"
      - think: 1
