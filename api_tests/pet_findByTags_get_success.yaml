# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=9124f669d9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_findByTags_get_success.csv"
    skipHeader: true
    fields:
      - "tags"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Scenario - Get Pets by Tags"
    flow:
      - log: "Starting request for /v2/pet/findByTags endpoint"
      - get:
          url: "/v2/pet/findByTags?tags={{tags}}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasHeader: "content-type"
      - think: 5
