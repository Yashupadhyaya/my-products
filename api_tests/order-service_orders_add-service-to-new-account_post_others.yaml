# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /order-service/orders/add-service-to-new-account_post for http method type POST in artillery framework
# RoostTestHash=5296e57867
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order-service_orders_add-service-to-new-account_post_others.csv"
    fields:
      - "channel"
      - "X-Trace-Id"
      - "username"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test invalid API usage scenarios"
    flow:
      - log: "Sending POST request with invalid payload"
      - post:
          url: "/invalid-endpoint-path"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending POST request without necessary headers"
      - post:
          url: "/order-service/orders/add-service-to-new-account"
          headers:
            channel: "{{ channel }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending POST request without authentication"
      - post:
          url: "/order-service/orders/add-service-to-new-account"
          headers:
            channel: "{{ channel }}"
            X-Trace-Id: "{{ X-Trace-Id }}"
            username: "{{ username }}"
          expect:
            - statusCode: 401
      - think: 1
