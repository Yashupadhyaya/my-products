# ********RoostGPT********

# Test generated by RoostGPT for test Test-5619 using AI Type  and AI Model 
# 
# Test generated for /.well-known/health_get for http method type GET in artillery framework
# RoostTestHash=b679c0374b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: ".well-known_health_get_others.csv"
    fields:
      - "Authorization"
      - "X-HSBC-Request-Correlation-Id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-success API requests test"
    flow:
      - log: "Sending GET request without authorization header"
      - get:
          url: "/.well-known/health"
          headers:
            X-HSBC-Request-Correlation-Id: "{{$randomUUID}}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending GET request with incorrect URL"
      - get:
          url: "/wrong-url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-HSBC-Request-Correlation-Id: "{{$randomUUID}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending GET request with incorrect Authorization header"
      - get:
          url: "/.well-known/health"
          headers:
            Authorization: "Bearer wrong_token"
            X-HSBC-Request-Correlation-Id: "{{$randomUUID}}"
          expect:
            - statusCode: 401
      - think: 1
