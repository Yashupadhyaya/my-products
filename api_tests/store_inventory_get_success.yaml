# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /store/inventory_get for http method type GET in artillery framework
# RoostTestHash=a66f7079af
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "store_inventory_get_success.csv"
    fields:
      - "field1"
      - "field2"
    delimiter: "^|^"
scenarios:
  - name: "PetStore inventory API test"
    flow:
      - log: "Performing GET request to fetch inventory"
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$"
            as: "responseBody"
      - log: "GET request performed successfully, asserting the response"
      - think: 1
      - assert:
            statusCode: 200
      - assert:
            hasProperty: "type"
      - assert:
            hasProperty: "additionalProperties"
      - assert:
            hasProperty: "additionalProperties.type"
      - assert: 
            hasProperty: "additionalProperties.format"
      - assert:
            equals: 
              - "{{ responseBody.type }}"
              - "object"
      - assert:
            equals:
              - "{{ responseBody.additionalProperties.type }}"
              - "integer"
      - assert: 
            equals:
              - "{{ responseBody.additionalProperties.format }}"
              - "int32"
      - log: "Assertions successful"
      - think: 1
