# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_get_success.csv"
    fields:
      - "name"
      - "jobTitle"
      - "email"
      - "id"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.acme-corp.com/"
      phases:
        - duration: 60
          arrivalRate: 10
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Search Employees"
    flow:
      - log: "Search for employees"
      - get:
          url: "/employees?id={{ id }}"
          headers:
            "security": "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 200
      - expect:
          - hasProperty: "id"
      - expect:
          - hasProperty: "name"
      - expect:
          - hasProperty: "jobTitle"
      - expect:
          - hasProperty: "email"
      - think: 5
