# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_get_success.csv"
    fields:
      - "id"
      - "jobTitle"
      - "name"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.host"
      auth_token: "devtoken"
    prod:
      target: "http://prod.api.host"
      auth_token: "prodtoken"

scenarios:
  - name: "Successful API responses"
    flow:
      - log: "Starting test for GET /employees"
      - get:
          url: "/employees"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          capture:
            json: "$.id"
            as: "employeeId"
          expect:
            statusCode: 200
            hasProperty: ["id", "jobTitle", "name", "email"]
      - think: 1
      - log: "Starting test for POST /employees"
      - post:
          url: "/employees"
          json:
            id: "{{ id }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            statusCode: 201
            hasProperty: ["id", "jobTitle", "name", "email"]
      - think: 1
      - log: "Starting test for PUT /employees/{{ employeeId }}"
      - put:
          url: "/employees/{{ employeeId }}"
          json:
            id: "{{ id }}"
            jobTitle: "{{ jobTitle }}"
            name: "{{ name }}"
            email: "{{ email }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            statusCode: 200
            hasProperty: ["id", "jobTitle", "name", "email"]
      - think: 1
      - log: "Starting test for DELETE /employees/{{ employeeId }}"
      - delete:
          url: "/employees/{{ employeeId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            statusCode: 204
      - think: 1
