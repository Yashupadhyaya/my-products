# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.vwvwvwv_API_HOST }}"
  plugins:
    expect: {}
  payloads:
    - path: "employees_get_success.csv"
      fields:
        - "id"
        - "jobTitle"
        - "name"
        - "email"
      skipHeader: true
      delimiter: "^|^"
  environments:
    local:
      target: "{{ $processEnvironment.vwvwvwv_API_HOST }}"
      AUTH_TOKEN: "{{$processEnvironment.vwvwvwv_AUTH_TOKEN}}"

scenarios:
    - name: "Test Employees Endpoints"
      flow:
        - log: "Retrieving all available employees"
        - get:
            url: "/employees"
            headers: 
              AUTH_TOKEN: "{{$processEnvironment.vwvwvwv_AUTH_TOKEN}}"
            expect:
              - statusCode: 200
              - hasProperty: 
                  path: ["employees"]
              - hasProperty:
                  path: ["id"]
              - equals: 
                  json: "{{ id }}"
              - hasProperty: 
                  path: ["jobTitle"]
              - equals: 
                  json: "{{ jobTitle }}"
              - hasProperty: 
                  path: ["name"]
              - equals: 
                  json: "{{ name }}"
              - hasProperty: 
                  path: ["email"]
              - equals: 
                  json: "{{ email }}"
        - think: 1
        - log: "Searching for existing employee"
        - get:
            url: "/employees?searchString={{ name }}"
            headers: 
              AUTH_TOKEN: "{{$processEnvironment.vwvwvwv_AUTH_TOKEN}}"
            expect:
              - statusCode: 200
              - hasProperty: 
                  path: ["employees"]
              - hasProperty:
                  path: ["id"]
              - equals: 
                  json: "{{ id }}"
              - hasProperty: 
                  path: ["jobTitle"]
              - equals: 
                  json: "{{ jobTitle }}"
              - hasProperty: 
                  path: ["name"]
              - equals: 
                  json: "{{ name }}"
              - hasProperty: 
                  path: ["email"]
              - equals: 
                  json: "{{ email }}"
        - think: 1
