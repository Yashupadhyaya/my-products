# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "http://api.example.com"
  plugins:
    expect: {}
  payload:
    path: "employees_get_success.csv"
    fields:
      - "email"
      - "jobTitle"
      - "name"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Testing success responses for employee APIs"
    flow:
      - log: "Starting GET /employees request"
      - get:
          url: "/employees?searchString={{ name }}"
          headers:
            Authorization: "Bearer {{ AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "jobTitle"
            - hasProperty: "name"
            - hasProperty: "email"
            - contentType: "application/json"
      - think: 1
