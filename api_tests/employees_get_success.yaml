# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees_get for http method type GET in artillery framework
# RoostTestHash=ec231a756b
# 
# 

# ********RoostGPT********
config:
  target: "{{processEnvironment.API_HOST}}"
  plugins:
    expect: {}
  payload:
    path: "employees_get_success.csv"
    fields:
      - "id"
      - "jobTitle"
      - "name"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.host"
      headers:
        Authorization: "Bearer {{processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Successful API requests scenario"
    flow:
      - log: 
          - "Starting scenario for successful API responses"
      - get:
          url: "/employees"
          headers:
            Authorization: "Bearer {{processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "jobTitle"
            - hasProperty: "name"
            - hasProperty: "email"
      - think: 
          - 5
      - log: 
          - "Finished scenario for successful API responses"
