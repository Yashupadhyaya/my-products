# ********RoostGPT********

# Test generated by RoostGPT for test api-spec-test-v1 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    fields:
      - "id"
    delimiter: "^|^"
scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "First, let's try an invalid ID"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Next, let's try without authorization"
      - get:
          url: "/employees/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Finally, let's try an incorrect URL"
      - get:
          url: "/employe/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
