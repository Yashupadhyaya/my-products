# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    fields:
      - validID
      - invalidID
    delimiter: "^|^"
    skipHeader: true
  
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Send an invalid GET request to /employees/{id}"
        
      - get:
          url: "/employees/invalidID"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        
      - expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "error"
            - equals:
                message: "Employee not found"
                error: "Not Found"

      - think: 1

      - log: "Send a GET request to /employee/{id} without authorization"
        
      - get:
          url: "/employees/validID"
          
      - expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                message: "Unauthorized"
                
      - think: 1

      - log: "Send a GET request to an undefined endpoint"
        
      - get:
          url: "/undefinedEndpoint"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        
      - expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals:
                message: "Undefined endpoint"
                
      - think: 1

      - log: "Send a GET request to /employees/{id} with invalid authorization token"
        
      - get:
          url: "/employees/validID"
          headers:
            authorization: "Bearer InvalidToken"
        
      - expect:
            - statusCode: 403
            - hasProperty: "message"
            - equals: 
                message: "Forbidden"
                
      - think: 1
