# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    fields:
      - "id"
    delimiter: "^|^"
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-successful API requests"
    flow:
      - log: "Attempting to get an employee with invalid id"
      - get:
          url: "/employees/{{ id }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to get an employee without authorization token"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Attempting to get an employee from non-existent endpoint"
      - get:
          url: "/employee/{{ id }}"
          expect:
            - statusCode: 404
      - think: 1
