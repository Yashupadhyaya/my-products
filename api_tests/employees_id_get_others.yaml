# ********RoostGPT********

# Test generated by RoostGPT for test testing-artillery using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.nwcwiuwc_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    fields: [ "id" ]
    skipHeader: true
    delimiter: "^|^"
  headers:
    Authorization: "Bearer {{$processEnvironment.nwcwiuwc_AUTH_TOKEN}}"
scenarios:
  - name: "Employee details retrieval scenario"
    flow:
      - log: "Sending GET request with invalid employee id"
      - get:
          url: "/employees/invalid_id"
          expect:
            - statusCode: 404
      - think: 3
      - log: "Sending GET request with valid but non-existing employee id"
      - get:
          url: "/employees/123e4567-e89b-42d3-a456-556642440000"
          expect:
            - statusCode: 204
      - think: 3
      - log: "Sending GET request without Authorization header"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: ""
          capture:
            json: "$"
          expect:
            - statusCode: 401
      - think: 3
      - log: "Sending GET request with correct id"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.nwcwiuwc_AUTH_TOKEN}}"
          capture:
            json: "$"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
            - hasProperty: "id"
