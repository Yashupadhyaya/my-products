# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-success API Response Test"
    flow:
      - log: "Attempting to get employee with invalid ID"
      - get:
          url: "/employees/{{ id }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to get employee with missing Authorization header"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to get employee with invalid Authorization token"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to get employee from non-existing endpoint"
      - get:
          url: "/non_existing_endpoint/{{ id }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
