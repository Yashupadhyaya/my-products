# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "http://example.com" # replace with your real target URL
  environments:
    local:
      target: "http://localhost" # replace with your real local target URL
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "employees_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"  # added delimiter field
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-successful API requests"
    flow:
      - log: "Attempting to get employee with invalid ID"
      - get:
          url: "/employees/{{ id }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to get employee without authorization"
      - get:
          url: "/employees/{{ id }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to get employee from non-existing endpoint"
      - get:
          url: "/non-existing-endpoint/{{ id }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
