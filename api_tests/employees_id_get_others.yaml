# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test using AI Type  and AI Model 
# 
# Test generated for /employees/{id}_get for http method type GET in artillery framework
# RoostTestHash=36bc161fb6
# 
# 

# ********RoostGPT********
config:
  target: "http://localhost"
  environments:
    local:
      target: "http://localhost"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.dqdbqwidbwibwc_AUTH_TOKEN}}"

scenarios:
  - name: "Non-successful API requests"
    flow:
      - log: "Attempting to get an employee with invalid id"
      - get:
          url: "/employees/invalid_id"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to get an employee without authorization"
      - get:
          url: "/employees/invalid_id"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Attempting to get an employee with invalid url"
      - get:
          url: "/invalid-url/invalid_id"
          expect:
            - statusCode: 404
      - think: 1
