# Test generated by RoostGPT for test testing-integration-test using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "scenario-587f2627bd.csv"
    fields:
      - "email"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:8080"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
        COMMIT_HASH: "928d28d"
        API_VERSION: "1.0.0"

scenarios:
  - name: "Create my account"
    flow:
      - post:
          url: "/accounts"
          headers:
            commit-hash: "{{ $environment.COMMIT_HASH }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "email"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "{{ $environment.API_VERSION }}"]
        log: "Created new account with email: {{ email }}"
        think: 1

      - get:
          url: "/me"
          headers:
            Token: "{{ $environment.AUTH_TOKEN }}"
            commit-hash: "{{ $environment.COMMIT_HASH }}"
          expect:
            - statusCode: 200
            - hasProperty: "email"
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "{{ $environment.API_VERSION }}"]
        log: "Verified account details for email: {{ email }}"
        think: 1
