# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=d905dac95c
# 
# 

# ********RoostGPT********
config:
  target: "{{ API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_login_get_others.csv"
    fields:
      - "username"
      - "password"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - flow:
    - log: "Sending an empty username"

    - get:
        url: "/v2/user/login?username={{ username }}&password={{ password }}"
        headers:
          api_key: "{{ AUTH_TOKEN }}"
        expect:
          - statusCode: 401

    - think: 1

    - log: "Sending an empty password"

    - get:
        url: "/v2/user/login?username={{ username }}&password={{ password }}"
        headers:
          api_key: "{{ AUTH_TOKEN }}"
        expect:
          - statusCode: 401

    - think: 1

    - log: "Sending invalid username and password"

    - get:
        url: "/v2/user/login?username=invalid&password=invalid"
        headers:
          api_key: "{{ AUTH_TOKEN }}"
        expect:
          - statusCode: 401

    - think: 1

    - log: "Sending request without api_key in headers"

    - get:
        url: "/v2/user/login?username={{ username }}&password={{ password }}"
        expect:
          - statusCode: 401

    - think: 1

    - log: "Sending request to invalid url"

    - get:
        url: "/v2/user/invalid_url?username={{ username }}&password={{ password }}"
        headers:
          api_key: "{{ AUTH_TOKEN }}"
        expect:
          - statusCode: 404

    - think: 1

