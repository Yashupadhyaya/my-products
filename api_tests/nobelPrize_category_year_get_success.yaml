# ********RoostGPT********

# Test generated by RoostGPT for test testingartillerytest using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /nobelPrize/{category}/{year}_get for http method type GET in artillery framework
# RoostTestHash=9818bf6b39
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_nobel_API_HOST }}"
  plugins:
    expect: {}
  payload:
    source: "./nobelPrize_category_year_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
  variables:
    authToken: "{{ $processEnvironment.url1_nobel_AUTH_TOKEN }}"

scenarios:
  - name: "Test Scenario for Nobel Prize Information API - Successful Responses"
    flow:
      - log:
          message: "Starting API tests for Nobel Prize data retrieval with valid inputs."
          
      - log:
          message: "Testing GET request for Nobel Prize category and year - expecting success."

      - get:
          url: "/2.1/nobelPrize/{{ category }}/{{ year }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "nobelPrize"
            - hasProperty: "nobelPrize.awardYear"
            - hasProperty: "nobelPrize.category.en"
            - hasProperty: "nobelPrize.category.se"
            - hasProperty: "nobelPrize.category.false"
            - hasProperty: "nobelPrize.categoryFullName.en"
            - hasProperty: "nobelPrize.categoryFullName.se"
            - hasProperty: "nobelPrize.categoryFullName.false"
            - hasProperty: "nobelPrize.dateAwarded"
            - hasProperty: "nobelPrize.prizeAmount"
            - hasProperty: "nobelPrize.prizeAmountAdjusted"
            - hasProperty: "nobelPrize.topMotivation.en"
            - hasProperty: "nobelPrize.topMotivation.se"
            - hasProperty: "nobelPrize.topMotivation.false"
            - hasProperty: "nobelPrize.laureates"
            - hasProperty: "nobelPrize.laureates[0].id"
            - hasProperty: "nobelPrize.laureates[0].name.en"
            - hasProperty: "nobelPrize.laureates[0].name.se"
            - hasProperty: "nobelPrize.laureates[0].name.false"
            - hasProperty: "nobelPrize.laureates[0].motivation.en"
            - hasProperty: "nobelPrize.laureates[0].motivation.se"
            - hasProperty: "nobelPrize.laureates[0].motivation.false"
            - hasProperty: "nobelPrize.laureates[0].links"
            - hasProperty: "nobelPrize.laureates[0].links[0].rel"
            - hasProperty: "nobelPrize.laureates[0].links[0].href"
            - hasProperty: "nobelPrize.laureates[0].links[0].types"

      - think:
          time: 2
