# ********RoostGPT********

# Test generated by RoostGPT for test artillery-test-gen using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/{petId}_get for http method type GET in artillery framework
# RoostTestHash=561990ad39
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - 
      duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_others.csv"
    fields: 
      - "petId"
    skipHeader: true
    delimiter: "^|^"
  
scenarios:
  -
    name: "Handling various non-successful API requests"
    flow:
      - log: "Sending an incorrect URL request..."
      - get:
          url: "/v2/peet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
      - think: 2
      - log: "Sending a request with an invalid petId parameter..."
      - get:
          url: "/v2/pet/9999999"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
      - think: 2
      - log: "Sending a request without authentication..."
      - get:
          url: "/v2/pet/{{ petId }}"
      - expect:
          - statusCode: 403
      - think: 2
      - log: "Sending a request with a valid petId but incorrect authentication token..."
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "invalid-auth-token"
      - expect:
          - statusCode: 403
      - think: 2
      - log: "Sending a request with a valid petId and valid authentication token..."
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "name"
      - think: 2
